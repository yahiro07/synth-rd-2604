export function m_abs(v: number) {
  return Math.abs(v);
}

export function m_sign(v: number) {
  return Math.sign(v);
}

export function m_min(a: number, b: number) {
  return Math.min(a, b);
}

export function m_max(a: number, b: number) {
  return Math.max(a, b);
}

export function m_clamp(v: number, lo: number, hi: number) {
  return Math.max(lo, Math.min(v, hi));
}

export function m_cos(v: number) {
  return Math.cos(v);
}

export function m_sin(v: number) {
  return Math.sin(v);
}

export function m_atan(v: number) {
  return Math.atan(v);
}

export function m_tanh(v: number) {
  return Math.tanh(v);
}

export function m_exp(v: number) {
  return Math.exp(v);
}

export function m_pow(a: number, b: number) {
  return Math.pow(a, b);
}

export function m_log(v: number) {
  return Math.log(v);
}

export function m_log2(v: number) {
  return Math.log2(v);
}

export function m_sqrt(v: number) {
  return Math.sqrt(v);
}

export function m_random() {
  return Math.random();
}

export function m_floor(v: number) {
  return Math.floor(v);
}

export function m_round(v: number) {
  return Math.round(v);
}

export function m_ceil(v: number) {
  return Math.ceil(v);
}

export function m_imul(a: number, b: number) {
  return Math.imul(a, b);
}

export const m_pi = Math.PI;
export const m_two_pi = Math.PI * 2;
export const m_half_pi = Math.PI * 0.5;

export const m_sqrt2 = Math.SQRT2;
